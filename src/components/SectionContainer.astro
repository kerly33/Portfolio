---
const { class: className, id } = Astro.props
---

<section id={id} class= {` ${className} w-full mx-auto lg:w-[740px] pb-24`}> <!-- Se puede poner asi: ${Astro.props.class} -->
    <slot />
</section>